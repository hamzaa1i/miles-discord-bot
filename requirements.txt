discord.py[voice]>=2.3.2
aiohttp>=3.9.0
python-dotenv>=1.0.0
flask>=3.0.0
Pillow>=10.0.0
pyfiglet>=1.0.2
PyNaCl>=1.5.0
qrcode>=7.4.2
yt-dlp>=2024.1.0
groq>=0.9.0
supabase>=2.0.0
sentry-sdk>=1.40.0
# PHASE N — multi-provider AI core (Python 3.11 compatible):
# google-genai  = current official Google Gemini SDK (successor of the
#                 deprecated google-generativeai package)
# mistralai    = Mistral's official SDK (v2 line; the adapter also
#                 supports the v1 import path)
# OpenRouter   = OpenAI-compatible REST via aiohttp (already a dep) —
#                 no additional HTTP library needed
google-genai>=1.0.0
mistralai>=1.5.0
